function calculateNeededDays(){
    //How many days of work needed to done all tasks ? // => 1 day = 9:00 to 00:00 between 00:00 and 09:00 doesnt count.
}

module.exports = {
    calculateNeededDays: calculateNeededDays
}